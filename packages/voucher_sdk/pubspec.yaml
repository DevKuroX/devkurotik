name: voucher_sdk
description: "DevKuroTik — Voucher bulk generation, payload assembly, Quick Print compatibility, and QR payload rules."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  # No dependencies yet — implementation begins in Phase 5

dev_dependencies:
  test: ^1.25.15
  lints: ^5.1.1
